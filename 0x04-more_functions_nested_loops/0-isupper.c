#include "main.h"
/**
 * main - Writes the function that checks for uppercase character.
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise.
 */
int _isupper(int c);
{
	
	if ((c >= 'A') && (c <= 'z'))
	return (1);

	else
	return (0);
}

