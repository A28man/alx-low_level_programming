#include <stdio.h>
/**
 * main - Writes the function that checks for uppercase character.
 * @c: input
 * Return: 1 for uppercase, 0 otherwise
 */
int _isupper(int c);
{
if ((c >= 'A') && (c <= 'Z'))

return (1);

else
return (0);
}
